// 3. Since foo is defined inside a block of curly braces, it is not accessible to the log output and so an error is created.

// 4.  You will received an error after it greets Vicotr 3 times, that you cannot change the constant NAME.

// 5. It logs 'bar' to the console because it i snever chaned in the scope where it is declared.

// 6. No error is run because the variable is never changed in teh outside scope, only within the block.
